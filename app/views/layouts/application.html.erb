<!DOCTYPE html>
<html>
  <head>
    <title>SmartFridgeAssistant</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "SmartFridge", root_path, class: "text-xl font-bold text-gray-800" %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "給食献立", school_lunches_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(school_lunches_path) ? 'border-indigo-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
              <%= link_to "食材管理", foods_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(foods_path) ? 'border-indigo-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
              <%= link_to "購入履歴", purchase_histories_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(purchase_histories_path) ? 'border-indigo-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
              <%= link_to "統計", statistics_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(statistics_path) ? 'border-indigo-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
              <%= link_to "学校行事", school_events_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(school_events_path) ? 'border-indigo-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" %>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <main>
      <%= yield %>
    </main>

    <%= yield :scripts %>
  </body>
</html>
